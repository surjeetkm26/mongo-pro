package com.api.service;

import org.springframework.stereotype.Service;

@Service("productService")
public class ProductService {

	/*
	 * @Autowired private MongoTemplate mongoTemplate;
	 * 
	 * public List<Product> getProduct(String name) { Query query=new Query();
	 * query.addCriteria(Criteria.where("productName").is(name)); List<Product>
	 * p=mongoTemplate.find(query, Product.class); System.out.println(p); return p;
	 * } public void updateProduct(String name) { Query query=new Query();
	 * query.addCriteria(Criteria.where("productName").is(name)); Update update=new
	 * Update(); //update.set(name, "") }
	 */
	
	
	
	
	
	
}
